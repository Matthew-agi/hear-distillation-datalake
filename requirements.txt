# Compatibility entrypoint for tools that still expect requirements.txt.
# The canonical dependency set lives in pyproject.toml; scripts/bootstrap.sh
# additionally selects the PyTorch wheel matching the host GPU driver.
-e .
